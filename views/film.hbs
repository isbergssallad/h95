<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>H95</title>
  <link rel="stylesheet" href="css/main.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>

<body>
  <div class="container justify-content-center align-items-center mt-2">
    <div class="">
      {{!-- Handlebars partials för navbar --}}
      {{> nav}}
      <div class="film-container align-items-center">
        <div class="d-flex flex-row gap-4 align-items-center justify-content-center">
          <img src="" alt="" id="poster" style="border-radius:3%">
          <div class="d-flex flex-column">
            <div class="title-container">
              <h3 id="title">Title</h3>
              <h5 id="year">Year</h5>
            </div>
            <div class="description-container">
              <h4 id="director">Directed by</h4>
              <p id="tagline"></p>
              <p id="description">Description</p>
            </div>
            <div class="film-buttons d-flex gap-2">

              <form action="/watchlist" method="POST">
                <input type="hidden" name="filmID" id="filmID" value="{{filmID}}">
                <input type="hidden" name="status" id="status" value="watched">
                {{!-- Ändra på knappens innehåll beroende på status --}}
                <button id="watched" class="button" type="submit" {{#if watched}}disabled{{/if}}>
                  {{#if watched}}
                  Watched!{{else}}
                  Watch
                  {{/if}}
                </button>

              </form>
              <form action="/watchlist" method="POST">
                <input type="hidden" name="filmID" id="filmID" value="{{filmID}}">
                <input type="hidden" name="status" id="status" value="planned">

                {{!-- Ändra på knappens innehåll beroende på status --}}
                <button id="add-watchlist" {{#if planned}}style="background-color:#D2C87C" {{/if}} class="button"
                  type="submit">
                  {{#if planned}}
                  Added to Watchlist
                  {{else}}
                  Add to Watchlist
                  {{/if}}
                </button>
              </form>
            </div>
            <div class="d-flex pt-3">
              <p>Posters and information are sourced from <a href="https://www.themoviedb.org/"
                  class="redirect">TMDB</a></p>
            </div>

          </div>
        </div>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
  <script src="js/film.js"></script>
</body>

</html>